# Archived scripts

これらは削除済みデータ、旧Python起動方法、旧DNN推論サーバーを参照する履歴資料。
実行しない。現行入口は次の4つ。

- `scripts/project_python.sh`
- `scripts/nnue_python.sh`
- `scripts/train_expert_blending.sh`
- `scripts/eval_accuracy.sh`
