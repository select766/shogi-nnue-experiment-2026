[project]
name = "train-nnue"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/train_nnue"]
